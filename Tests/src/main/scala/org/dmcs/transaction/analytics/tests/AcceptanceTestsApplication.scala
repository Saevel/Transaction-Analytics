package org.dmcs.transaction.analytics.tests

object AcceptanceTestsApplication extends App {
/*
  val tests: Seq[DataDrivenTest[_, _]] = Seq.empty

  val timeout: FiniteDuration = ???

  Await.result(tests.foldLeft[Future[_]](Future.successful({}))((aggregate, test) =>
    aggregate.flatMap(_ => test.execute)
  ), timeout)
  */
}